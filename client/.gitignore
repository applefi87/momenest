*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
build/
/captures
.externalNativeBuild
.cxx
local.properties

# Gradle wrapper 的 jar 是二進位檔，由 `gradle wrapper` 或 Android Studio 產生
# （若團隊要求 wrapper 進版控，把下面這行刪掉並確實 commit 該 jar）
gradle/wrapper/gradle-wrapper.jar
